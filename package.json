{
  "name": "chrome-extension-search-history",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --clearScreen false",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format": "pnpm exec prettier . --write"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^1.0.14",
    "@types/chrome": "^0.0.246",
    "@types/node": "^20.11.20",
    "prettier": "3.2.5",
    "typescript": "^4.5.4",
    "vite": "^2.9.15"
  },
  "dependencies": {
    "@sentry/browser": "latest"
  }
}
